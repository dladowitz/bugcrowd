## Bugcrowd Challenge

### Routes

-  /tags (POST) 
-  /tags/:entity_identifier (GET)
-  /tags/:entity_identifier (DELETE)
-  /stats (GET)
-  /stats/:entity_identifier (GET)

<hr>
### URL Schema
I decided that an entity_identifier would be unique accross entities. So this made it enough soley idenitfy an entity with. This simplied the URL schema. 
<hr>

### Tests
All modes and controllers have working RSpec tests. 




